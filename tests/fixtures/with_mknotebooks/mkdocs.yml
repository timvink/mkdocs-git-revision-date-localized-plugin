site_name: demo revision date
use_directory_urls: false

theme:
    name: material

plugins:
    - search
    - git-revision-date-localized:
            type: timeago
    - mknotebooks:
        exporter_kwargs:
            exclude_input: true