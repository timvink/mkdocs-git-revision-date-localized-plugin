site_name: test gitrevisiondatelocalized_plugin
use_directory_urls: true

theme:
    name: 'material'
    features:
        - instant

plugins:
    - search
    - git-revision-date-localized:
        type: timeago